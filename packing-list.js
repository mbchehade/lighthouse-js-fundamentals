const packListing = ["bowls", "plates", "pots", "eating utensils", "glasses", "cups", "cook utensils"];

console.log("Kitchen stuff to pack:");
let i = 0;
while (i < packingList.length) {
  console.log(packingList[i]);
  i++;
}
